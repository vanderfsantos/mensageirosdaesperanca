import NewsForm from '@/components/admin/NewsForm';

export const metadata = { title: 'Nova Notícia | Admin — Mensageiros da Esperança' };

export default function NovaNoticiaPage() {
  return <NewsForm />;
}
