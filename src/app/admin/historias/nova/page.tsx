import StoryForm from '@/components/admin/StoryForm';

export const metadata = { title: 'Nova História | Admin — Mensageiros da Esperança' };

export default function NovaHistoriaPage() {
  return <StoryForm />;
}
